const GET_MESSAGES = Symbol("GET_MESSAGES");

export default function reducer(state = {}, action = {}) {
  switch (action.type) {
    // do reducer stuff
    default:
      return state;
  }
}


export function getMessages() {
  
}